<!-- src/components/Nav.astro -->
<style>
    .menu-icon {
            display: none; /* Show menu icon */
            font-size: 30px;
            color: black;
            background: none;
            border: none;
            cursor: pointer;
            position: absolute;
            top: 20px;
            right: 20px;
            z-index: 1000;
    }
    .top-right-nav {
      display: flex !important;
      position: absolute;
      top: 0;
      right: 0;
      margin: 20px; /* Adjust based on desired spacing from the corners */
      color: black;
	  font-family: 'Urbanist';
	  font-size: 18px;
      display: flex;
      align-items: center;
      justify-content: flex-end;
	  z-index: 500;
    }
	.top-right-nav a {
		margin-left: 40px; /* Space between links */
		text-decoration: none;
		color: black; /* Adjust if needed to ensure visibility */
	}
    .hidden {
    display: none !important;
    }

    @media (max-width: 768px) {
        .menu-icon {
            display: block; /* Show menu icon */
            position: absolute;
            top: 20px;
            right: 20px;
            z-index: 1000;
        }
        .top-right-nav {
            display: none; /* Hide navigation links */
            flex-direction: column; /* Stack the links vertically */
            position: fixed; /* Fixed position */
            top: 0; right: 0; /* Positioning */
            background-color: rgba(255, 255, 255, 1); /* set last value (alpha) as 0.9 for semi-transparent background */
            width: 150px;
            height: 100vh; /* set as 95.3vh to just touch bottom of screen and show bottom rounded corner*/
            justify-content: flex-start; /* Align links to the top */
            padding-top: 50px; /* Space for the menu icon */
            margin-right: 0;
            margin-top: 0;
            border-radius: 0px 0px 0px 0px; /* set first and last values as 15px for rounded corners on the bottom-left */
        }
        .top-right-nav a {
            margin-left: 0px; /* Space between links */
            margin-top: 30px;
            text-decoration: none;
            padding: 0 10px; /* Padding around links for better touch targets on touchscreens */
            color: black; /* Adjust if needed to ensure visibility */
        }
    }  
</style>


<button class="menu-icon" aria-label="Open navigation menu">
    ☰
</button>
  

<nav class="top-right-nav">
    <a href="/">HOME</a>
    <a href="/blog">BLOG</a> 
    <a href="/travel">TRAVEL TIPS</a> 
    <a href="/articles">ARTICLES</a> 
    <a href="/reviews">REVIEWS</a> 
    <!-- <a href="/about">ABOUT</a> -->
</nav>


<script>
    document.addEventListener('DOMContentLoaded', () => {
        const menuIcon = document.querySelector('.menu-icon');
        const topRightNav = document.querySelector('.top-right-nav');

        // Function to toggle the top-right navigation
        function toggleNavigation() {
            topRightNav.classList.toggle('hidden');
        }

        // Function to set the default state of the navigation menu
        function setDefaultNavState() {
            if (window.innerWidth <= 768) {
                topRightNav.classList.add('hidden');
            } else {
                topRightNav.classList.remove('hidden');
            }
        }

        // Set the initial state on DOMContentLoaded
        setDefaultNavState();

        // Event listener for the menu icon click
        menuIcon.addEventListener('click', toggleNavigation);

        // Event listener to handle window resize
        window.addEventListener('resize', () => {
            setDefaultNavState();
        });
    });
</script>